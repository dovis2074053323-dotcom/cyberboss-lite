# The bot may invoke exactly one root-owned helper as keke. The helper grants
# only a read ACL on the canonical Claude OAuth credential file; it accepts no
# arguments and never writes credential contents.
wechatbot ALL=(keke) NOPASSWD: /usr/local/sbin/wechat-claude-ensure-credential-acl ""
