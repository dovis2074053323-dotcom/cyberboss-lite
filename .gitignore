node_modules/
.DS_Store
.env
.env.*
.mcp.json
.claude/settings.json
.claude/settings.local.json
npm-debug.log*
third_party/
firmware/

# Defense-in-depth: runtime state normally lives outside the repo entirely,
# but never let credentials/tokens/state land in git even if that is
# misconfigured locally.
*.credentials.json
sender-allowlist.json
accounts/
sync-buffers/
claude-cfg/
/state/
.wechat-claude-bot/
