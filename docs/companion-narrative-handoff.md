# Companion narrative timeline handoff

## 2026-08-17 — Codex（栖）

- Scope: restore the server-side companion narrative boundary and make Cyberboss lite consume that boundary. Clawd Android, automatic `freshContext`, Event Opportunity time detection, and the existing PrivacyGate rules were not changed.
- The live producer is `/home/keke/scripts/keke-companion-aggregate.py`, run by keke's crontab at minute 5 of every hour with a 2-hour window. There is no companion systemd timer. `/home/keke/scripts/keke-companion-summary.py today` remains the second, story-oriented daily renderer used by the existing helper/Breath path; it is not the aggregation source.
- Aggregate v7 keeps raw `companion_events` append-only, filters sensitive screen contexts with the existing server-side gate, ignores launcher/keyboard/system noise, merges page/search hops, splits on screen boundaries and long idle, and emits only `category, duration, active-state` summaries plus bounded interaction counts. A stable category change is approximately 15 minutes; it is not a page-change splitter. Before editing, backups were saved as `keke-companion-aggregate.py.pre-narrative-20260817` and `keke-companion-summary.py.pre-narrative-20260817`.
- Today baseline before the writeback: 709 raw events, 63 segments, 691 context items, 63 non-empty summaries, 87,014 joined JSON characters / 63 rows; `summary.py today` read 63 segments and produced 221 characters / 24 lines. After the 18-hour boundary rebuild: 742 raw events (new events arrived while validating), 13 today segments, 0 context items, 13 non-empty summaries, 2,219 joined JSON characters / 13 rows; `summary.py today` produced 628 characters / 72 lines. The final summaries contain no package/activity/title/url/query/raw-context tokens and no exact duplicate ranges.
- Cyberboss `lite` now queries and projects only `start_ts`, `end_ts`, `summary`, `screen_active`, and bounded interaction counts. The proactive prompt renders at most eight recent rows as `Recent companion timeline`; legacy verbose summaries are replaced server-side with `activity summary unavailable` before they can reach the prompt. Forced and optional proactive paths continue to use the same single-turn builder.
- Verification: Cyberboss targeted observation/privacy/proactive/Event Opportunity tests pass; full suite is 240/241 with the one pre-existing `proactive-budget-store.test.js` failure (`pre-runtime reservation can be released without a phantom call`), not introduced by this change. The initial sandbox run's unrelated `/tmp` EROFS was rerun in a writable test environment. A live-code protocol probe confirmed the observation projection has no `contexts` and the prompt has no package/activity/title/url/raw detail. `cyberboss.service` restarted successfully at 17:18:59 CST and is active.
- Git: this handoff and the owned Cyberboss source/tests are to be committed and pushed on `lite`. Existing credential-sync changes in `deploy/`, `src/core/app.js`, `src/adapters/runtime/claudecode/index.js`, `docs/cyberboss-lite-status.md`, `src/core/auth-health.js`, and `test/credential-sync.test.js` belong to another overlay and must remain un-staged.
